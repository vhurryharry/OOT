{% extends 'shell.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block body %}
  <section class="section">
    <div class="container">
      <h1 class="title">
        {{ page.title }}
      </h1>

      {{ content }}
    </div>
  </section>
{% endblock %}
